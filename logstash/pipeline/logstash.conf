input {
  beats {
    port => 5044
  }
}

filter {
  mutate {
    rename => { "msg" => "message" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "orders-service-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
